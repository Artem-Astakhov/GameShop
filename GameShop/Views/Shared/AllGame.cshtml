@model GameShop.Models.Game

    <div class="col-lg-4">
        <img class="img-thumbnail" src="@Model.Image" width="200" height="200" alt=" @Model.Name" />
        <h2>@Model.Name</h2>
        <p>Цена: @Model.Prise.ToString("c")</p>
        <p><a class="btn btn-warning" href="#">Подробнее</a></p>
    </div>